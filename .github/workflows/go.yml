name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  GOPATH: ${{ github.workspace }}/go
  GOROOT: /usr/local/go
  PATH: ${{ github.workspace }}/go/bin:${{ github.workspace }}/go/pkg/mod/cache/download:${{ github.workspace }}/go/bin:${{ github.env.PATH }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install tar
      run: apt-get update && sudo apt-get install -y tar
    
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.22.0

    - name: Build
      run: go build -v ./...

    - name: Run tests
      run: go test -v ./...
